using ProjectAtipax.Models;

namespace ProjectAtipax.DAO
{
    public class accesoDAO
    {
       /* public List<Usuario> lstUsuario()
        {
            return new List<Usuario>{
             new Usuario{idUsuario=1, usuario="admi@gmail.com", pass="admi", roles= new string[]{"Administrador"}},
             new Usuario{idUsuario=2, usuario="cliente@gmail.com", pass="cliente", roles= new string[]{"Cliente"}}

            };
        }

        public Usuario validacion(string usu, string pas)
        {
            return lstUsuario().Where(item => item.usuario == usu && item.pass == pas);
        }*/
    }
}
